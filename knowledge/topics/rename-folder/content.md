```bash
mv -i -- OLD_NAME NEW_NAME
```

`mv` stands for **move**. Linux uses the same command to move and rename
files and directories.

## Example

```bash
mv -i -- "photos" "pictures"
```

`-i` asks before replacing an existing destination. Inspect both paths before
confirming an overwrite.
